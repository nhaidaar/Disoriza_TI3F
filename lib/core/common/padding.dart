import 'package:flutter/material.dart';

class Padding {
  static const EdgeInsets xSmall = EdgeInsets.all(12.0);
  static const EdgeInsets small = EdgeInsets.all(16.0);
  static const EdgeInsets medium = EdgeInsets.all(20.0);
  static const EdgeInsets large = EdgeInsets.all(40.0);
}
